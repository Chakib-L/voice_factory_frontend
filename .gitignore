# Dossier des dépendances npm
/node_modules

# Fichiers de log de l'outil npm (rarement utiles à commiter)
npm-debug.log*
.npm

# Dossiers de build/distribution
# Cela dépend de votre bundler ou environnement (Webpack, Rollup, Babel, TypeScript)
/dist
/build
/out
/release

# Fichiers d'environnement
# Contiennent des secrets ou des configurations spécifiques à l'environnement local
.env
.env.local
.env.*.local
.vscode/*.env*

# Fichiers de cache de l'IDE / OS
.DS_Store
Thumbs.db

# Fichiers de cache et temporaires générés par Node
/tmp
*.pid
*.seed
*.log
/logs

# Fichiers générés par TypeScript
# La sortie JS compilée ne doit pas être commitée si vous commitez le TS source
*.tsbuildinfo

# Cache de l'exécution Node.js (V8)
*.tgz
*.zip
.eslintcache
.nyc_output
.cache

# Fichiers d'historique de l'éditeur de texte (ex: Vim, Emacs)
*~
#*#

# Fichiers générés par des outils spécifiques
# Par exemple, pour des applications utilisant Docker
docker-compose.local.yml

# Fichiers de couverture de test
/coverage
